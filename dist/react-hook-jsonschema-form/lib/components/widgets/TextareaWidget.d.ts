import { CommonProps } from "schema-form-core";
export { TextareaWidget };
declare function TextareaWidget({
  name,
  required,
  methods,
  schema,
  uiSchema,
  disabled,
}: CommonProps): JSX.Element;
