import { CommonProps } from "schema-form-core";
export { CheckboxWidget };
declare function CheckboxWidget({
  name,
  schema,
  disabled,
  readonly,
  label,
  required,
  DescriptionField,
  methods,
}: CommonProps): JSX.Element;
