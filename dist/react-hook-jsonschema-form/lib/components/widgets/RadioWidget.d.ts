import { CommonProps } from "schema-form-core";
export { RadioWidget };
declare function RadioWidget({
  id,
  disabled,
  options,
  readonly,
  name,
  methods,
  schema,
  required,
}: CommonProps): JSX.Element;
