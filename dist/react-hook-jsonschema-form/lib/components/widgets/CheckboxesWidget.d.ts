import { CommonProps } from "schema-form-core";
export { CheckboxesWidget };
declare function CheckboxesWidget({
  id,
  disabled,
  options,
  readonly,
  name,
  methods,
  schema,
  required,
}: CommonProps): JSX.Element;
