import { CommonProps } from "schema-form-core";
declare const StringField: ({
  name,
  schema,
  uiSchema,
  required,
  registry,
  methods,
  ...rest
}: CommonProps) => JSX.Element;
export default StringField;
