import { CommonProps } from "schema-form-core";
declare const ArrayField: ({
  name,
  registry,
  schema,
  idSchema,
  uiSchema,
  required,
  readonly,
  disabled,
  idPrefix,
  idSeparator,
  methods,
  placeholder,
}: CommonProps) => JSX.Element;
export default ArrayField;
