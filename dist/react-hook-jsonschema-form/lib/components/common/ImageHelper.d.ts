type Props = {
    uiSchema: {
        title?: string;
        sources: Array<string>;
    };
};
declare const ImageHelper: ({ uiSchema }: Props) => JSX.Element | null;
export default ImageHelper;
